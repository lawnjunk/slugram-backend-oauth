'use strict';

module.exports = ['$q', '$log', '$http', '$window', authService];

function authService($q, $log, $http, $window){
  $log.log('init authService');

  let service = {};

  // request oauth page

  return service;
}
